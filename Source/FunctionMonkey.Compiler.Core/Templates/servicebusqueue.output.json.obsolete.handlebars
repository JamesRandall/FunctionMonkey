// This is no longer needed (and if you include it it actually causes an error) but I want to leave for reference
{
    "type": "serviceBusTrigger",
    "connection": "{{ConnectionStringSettingName}}",
    "queueName": "{{QueueName}}",
    "name": "collector",
    "direction": "out"
}
